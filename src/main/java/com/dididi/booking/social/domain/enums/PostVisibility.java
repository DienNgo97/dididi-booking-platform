package com.dididi.booking.social.domain.enums;

/** Quyen xem bai: PUBLIC = moi thanh vien; FOLLOWERS = chi nguoi theo doi; PRIVATE = chi tac gia. */
public enum PostVisibility {
    PUBLIC,
    FOLLOWERS,
    PRIVATE
}
