package com.dididi.booking.social.domain.enums;

/** Quyen rieng tu trang ca nhan: PUBLIC = moi thanh vien xem; PRIVATE = chi follower (duyet). */
public enum ProfileVisibility {
    PUBLIC,
    PRIVATE
}
